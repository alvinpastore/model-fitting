rows = 10
cols = 20

gw = [[((rows-1-x1)+(cols-1-x2)) for x1 in range(rows)] for x2 in range(cols)]

tot = 0
for elem in gw:
	print elem
	tot += sum(elem)
	
print tot / (rows*cols)

print (rows/2) + (cols/2) -1
